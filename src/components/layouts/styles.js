import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles((theme) => ({
    root: {
      display: 'flex',
      height: '100vh',
      flexGrow: 1,
      position: 'inherit',
      overflow: 'auto',

    },
  }));

